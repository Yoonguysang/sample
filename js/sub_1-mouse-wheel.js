$(function () {
    const mainSlider = $(".slider");
    const sliderCounter = $(".slider-counter");
    const sliderProgress = $(".progress-bar");

    let currentSlide;
    let nextSlide;
    let totalSlideCount;
    let calc;






});
